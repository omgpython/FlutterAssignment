Question -> What will happen if a class implements two interfaces and they both have a method with same name and signature?

-> The class have can implement both interface method with a single concrete method.